# Google Gemini API Key (required)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# Use Google AI Studio (not Vertex AI)
GOOGLE_GENAI_USE_VERTEXAI=FALSE

# ============================================================================
# Interview Agent Configuration
# ============================================================================
# The interview agent supports both remote A2A agents and a free default agent.
#
# FREE DEFAULT AGENT:
# - Always available: "default" company with "system_design" interview type
# - No configuration needed - works out of the box
# - Provides a quality system design interview experience using keyword-based evaluation
# - User must explicitly choose "default" to use this free option
#
# REMOTE AGENTS (optional, may incur costs):
# Configure remote agents using the variables below. Remote agents provide
# specialized, company-specific interview experiences via the A2A protocol.
# These may incur costs depending on your setup.
#
# Comma-separated list of remote agents to configure (optional)
# If not set, only the free default agent will be available
INTERVIEW_AGENTS=google,meta

# Google Agent Configuration (remote)
GOOGLE_AGENT_URL=http://localhost:10123
GOOGLE_AGENT_TYPES=system_design,coding
GOOGLE_AGENT_DESCRIPTION=Google-style interviewer

# Meta Agent Configuration (remote)
META_AGENT_URL=http://localhost:10125
META_AGENT_TYPES=system_design
META_AGENT_DESCRIPTION=Meta-style interviewer using LangGraph

# Add more remote agents by following this pattern:
# {AGENT_NAME}_AGENT_URL=http://localhost:port
# {AGENT_NAME}_AGENT_TYPES=comma,separated,types
# {AGENT_NAME}_AGENT_DESCRIPTION=Description (optional)
